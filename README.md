# Hacking-

## Project Structure

### `scripts/`

The `scripts` directory contains utility scripts for the project.

- **hello.py** - A simple hello world script. Run it with:

  ```bash
  python scripts/hello.py
  ```
